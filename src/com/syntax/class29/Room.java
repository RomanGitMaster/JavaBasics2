package com.syntax.class29;

 interface Room {
     interface Car{

     }
}
class Test{
    public static void main(String[] args) {
       // Room.Car the way we specify that interface Car is nested in Room interface
    }
}
